/**
 * FUTURE RELATION: orders.settings_id should usually be derived through the branch/restaurant context instead of being stored directly.
 * FUTURE RELATION: reservations should resolve branch business-hours and special-hours together with reservation-rules.
 * FUTURE RELATION: receipt rendering should bind device-printer-profiles to settings-receipts defaults.
 */
package pos.pos.settings.entity;
